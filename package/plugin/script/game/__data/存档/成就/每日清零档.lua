--------------------------------------------------------------------------------------
-- DAILY_COMPLETE_REWARD_DAY_MAX1
local name = INT_STORAGE.DAILY_COMPLETE_REWARD_DAY_MAX1
local mt = {
    name = name,
    dayMaxName = "每日通关奖励次数上限1",
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)
--------------------------------------------------------------------------------------
-- DAILY_COMPLETE_REWARD_DAY_MAX3
local name = INT_STORAGE.DAILY_COMPLETE_REWARD_DAY_MAX3
local mt = {
    name = name,
    dayMaxName = "每日通关奖励次数上限3",
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)
--------------------------------------------------------------------------------------
-- DAILY_COMPLETE_REWARD_DAY_MAX5
local name = INT_STORAGE.DAILY_COMPLETE_REWARD_DAY_MAX5
local mt = {
    name = name,
    dayMaxName = "每日通关奖励次数上限5",
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)
--------------------------------------------------------------------------------------
-- DAILY_COMPLETE_REWARD_DAY_MAX8
local name = INT_STORAGE.DAILY_COMPLETE_REWARD_DAY_MAX7
local mt = {
    name = name,
    dayMaxName = "每日通关奖励次数上限7",
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)
--------------------------------------------------------------------------------------
-- DAILY_ARCH_BOSS_REWARD_DAY_MAX
local name = INT_STORAGE.DAILY_ARCH_BOSS_REWARD_DAY_MAX
local mt = {
    name = name,
    dayMaxName = "存档BOSS每日通关奖励次数上限",
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)
--------------------------------------------------------------------------------------
-- 通关次数（日限）
local name = INT_STORAGE.WIN_COUNT_DAY
local mt = {
    name = name,
    dayMaxName = '每日通关次数',
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)
--------------------------------------------------------------------------------------
-- 核心
local name = INT_STORAGE.CORE_GET_DAY
local mt = {
    name = name,
    dayMaxName = '每日核心获取',
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)
--------------------------------------------------------------------------------------
-- 挂机奖励
local name = INT_STORAGE.HUNT_SCORE_DAY
local mt = {
    name = name,
    dayMaxName = '每日挂机积分',
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)
--------------------------------------------------------------------------------------
-- 神器碎片日上限
local name = INT_STORAGE.ARTIFACT_SHARD_DAY
local mt = {
    name = name,
    dayMaxName = '神器碎片日上限',
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)
--------------------------------------------------------------------------------------
-- 神器经验日上限
local name = INT_STORAGE.ARTIFACT_PLAYER_EXP_DAY
local mt = {
    name = name,
    dayMaxName = '神器碎片日上限',
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)
--------------------------------------------------------------------------------------
-- 护肝礼包
local name = INT_STORAGE.HU_GAN_LI_BAO_DAY
local mt = {
    name = name,
    dayMaxName = '护肝礼包今日生效记录',
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)
--------------------------------------------------------------------------------------
-- 每日神器兑换数
local name = INT_STORAGE.ARTIFACT_EXCHANGE_1_DAY
local mt = {
    name = name,
    dayMaxName = '每日神器兑换数',
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)
--------------------------------------------------------------------------------------
-- 每日神器兑换数2
local name = INT_STORAGE.ARTIFACT_EXCHANGE_2_DAY
local mt = {
    name = name,
    dayMaxName = '每日神器兑换数2',
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)
--------------------------------------------------------------------------------------
-- 每日神器兑换数3
local name = INT_STORAGE.ARTIFACT_EXCHANGE_3_DAY
local mt = {
    name = name,
    dayMaxName = '每日神器兑换数2',
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)
--------------------------------------------------------------------------------------
-- 每日神器兑换数4
local name = INT_STORAGE.ARTIFACT_EXCHANGE_4_DAY
local mt = {
    name = name,
    dayMaxName = '每日神器兑换数4',
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)
--------------------------------------------------------------------------------------
-- 每日藏品碎片获取数
local name = INT_STORAGE.COLLECTION_SHARD_DAY
local mt = {
    name = name,
    dayMaxName = '每日藏品碎片获取数',
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)
--------------------------------------------------------------------------------------
-- 每日狩猎币获取数
local name = INT_STORAGE.HUNT_SHARD_DAY
local mt = {
    name = name,
    dayMaxName = '每日狩猎币获取数',
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)
--------------------------------------------------------------------------------------
-- 每日狩猎印记经验获取数
local name = INT_STORAGE.HUNTING_MARK_EXP_DAY
local mt = {
    name = name,
    dayMaxName = '每日狩猎印记经验获取数',
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)
--------------------------------------------------------------------------------------
-- 每日圣物材料获取数
local name = INT_STORAGE.HOLY_ITEM_MATERIAL_GET_DAY
local mt = {
    name = name,
    dayMaxName = '每日圣物材料获取数',
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)
--------------------------------------------------------------------------------------
-- MENTORSHIP_PROGRAM_COIN_DAY_MAX_GAIN
local name = INT_STORAGE.MENTORSHIP_PROGRAM_COIN_DAY_MAX_GAIN
local mt = {
    name = name,
    dayMaxName = "老带新信物点每日获取上限",
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)
--------------------------------------------------------------------------------------
-- 英雄熟练度（日上限）
local name = INT_STORAGE.HERO_EXP_GET_DAY
local mt = {
    name = name,
    dayMaxName = '英雄经验',
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)

--------------------------------------------------------------------------------------
-- 新登录天数（日上限）
local name = INT_STORAGE.LOGIN_COUNT_DAY
local mt = {
    name = name,
    dayMaxName = '新登录天数',
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)
--------------------------------------------------------------------------------------
-- 新登录天数（日上限）522
local name = INT_STORAGE.LOGIN_COUNT_DAY_522
local mt = {
    name = name,
    dayMaxName = '新登录天数522',
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)

--------------------------------------------------------------------------------------
-- 第一章通行证经验（日上限）
local name = INT_STORAGE.BATTLEPASS_1_EXP_DAY
local mt = {
    name = name,
    dayMaxName = '通行证经验',
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)
--------------------------------------------------------------------------------------
-- 无尽代币（日上限）
local name = INT_STORAGE.ENDLESS_SHARD_DAY
local mt = {
    name = name,
    dayMaxName = '无尽代币',
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)

--------------------------------------------------------------------------------------
-- 金坷垃（日上限）
local name = INT_STORAGE.JIN_KE_LA_DAY
local mt = {
    name = name,
    dayMaxName = '金坷垃（日上限）',
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)
--------------------------------------------------------------------------------------
-- 解锁种子（日上限）
local name = INT_STORAGE.SEED_UNLOCKED_COUNT_DAY
local mt = {
    name = name,
    dayMaxName = '解锁种子（日上限）',
    hideLimit = true,
}
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)
--------------------------------------------------------------------------------------
-- 青团获取数（日上限）
local name = INT_STORAGE.QING_TUAN_DAY
local mt = {
    name = name,
    dayMaxName = '青团获取数',
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)

--------------------------------------------------------------------------------------
-- 存档怪物1击杀数（日上限）
local name = INT_STORAGE.STRONGER_CHALLENGE_BOSS_1_DAY
local mt = {
    name = name,
    dayMaxName = '存档怪物1击杀数（日上限）',
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)

--------------------------------------------------------------------------------------
-- 存档怪物2击杀数（日上限）
local name = INT_STORAGE.STRONGER_CHALLENGE_BOSS_2_DAY
local mt = {
    name = name,
    dayMaxName = '存档怪物1击杀数（日上限）',
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)

--------------------------------------------------------------------------------------
-- 复活节碎片获取（日上限）
local name = INT_STORAGE.EASTER_SHARD_DAY_GET
local mt = {
    name = name,
    dayMaxName = '复活节碎片获取（日上限）',
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)

--------------------------------------------------------------------------------------
-- 科技点（日上限）
local name = INT_STORAGE.TECH_POINT_GET_DAY
local mt = {
    name = name,
    dayMaxName = '科技点（日上限）',
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)
--------------------------------------------------------------------------------------
-- 520碎片（日上线）
local name = INT_STORAGE.GODDESS_FLOWER_SHARD_GET_DAY
local mt = {
    name = name,
    dayMaxName = '520鲜花（日上线）',
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)

--------------------------------------------------------------------------------------
-- 端午积分（日上线）
local name = INT_STORAGE.DUAN_WU_POINT_GET_DAY
local mt = {
    name = name,
    dayMaxName = '端午积分（日上线）',
    hideLimit = true,
}
--------------------------------------------------------------------------------------
reg:initTableData('archData', mt)
reg:addToPool('arch_day_clear_list', mt)