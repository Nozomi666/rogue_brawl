local prefix = 'game.__data.状态.通用.'

require (prefix .. '眩晕')
require (prefix .. '沉默')
require (prefix .. '召唤物')
require (prefix .. '持续满能')
require (prefix .. '免疫')
require (prefix .. '冰冻')
require (prefix .. '缠绕')


require (prefix .. '禁锢')
require (prefix .. '控制免疫')
require (prefix .. '暂停')
require (prefix .. '物理免疫')
require (prefix .. '魔法免疫')

local prefix = 'game.__data.状态.其他.'

